<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oliveoa.view"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/home"
            android:theme="@style/AppTheme">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/LoginTheme" />
        <!--<activity
            android:name=".AdhibitionActivity"
            android:label="@string/adhibition"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".notice.NoticeActivity"
            android:label="@string/notice"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".addressbook.AddressBookActivity"
            android:label="@string/address_book"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".mine.MineActivity"
            android:label="@string/mine"
            android:theme="@style/AppTheme" />
        <activity android:name=".TabLayoutBottomActivity"/>-->
        <activity
            android:name=".TabLayoutBottomActivity"
            android:theme="@style/AppTheme"
            android:label="@string/home_name"
            android:allowBackup="true"
            android:supportsRtl="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>

</manifest>